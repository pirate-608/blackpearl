# Initial fallback only. Prefer /connect in the TUI.
BLACKPEARL_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=
OPENAI_MODEL=
# responses | chat_completions
OPENAI_API_MODE=responses
AGENT_MAX_STEPS=6
